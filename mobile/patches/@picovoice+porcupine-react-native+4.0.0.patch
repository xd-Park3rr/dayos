diff --git a/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupineModule.java b/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupineModule.java
index 3575426..1c2efcb 100644
--- a/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupineModule.java
+++ b/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupineModule.java
@@ -22,6 +22,7 @@ import com.facebook.react.bridge.ReactMethod;
 import com.facebook.react.bridge.ReadableArray;
 import com.facebook.react.bridge.WritableArray;
 import com.facebook.react.bridge.WritableMap;
+import com.facebook.react.module.annotations.ReactModule;
 
 import java.io.File;
 import java.util.ArrayList;
@@ -30,6 +31,7 @@ import java.util.Map;
 import java.util.Locale;
 
 
+@ReactModule(name = "PvPorcupine")
 public class PorcupineModule extends ReactContextBaseJavaModule {
 
     private static final String LOG_TAG = "PvPorcupine";
diff --git a/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupinePackage.java b/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupinePackage.java
index a8d3a5a..d9a0970 100644
--- a/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupinePackage.java
+++ b/node_modules/@picovoice/porcupine-react-native/android/src/main/java/ai/picovoice/reactnative/porcupine/PorcupinePackage.java
@@ -1,25 +1,56 @@
 package ai.picovoice.reactnative.porcupine;
 
-import com.facebook.react.ReactPackage;
+import androidx.annotation.NonNull;
+import androidx.annotation.Nullable;
+
+import com.facebook.react.TurboReactPackage;
 import com.facebook.react.bridge.NativeModule;
 import com.facebook.react.bridge.ReactApplicationContext;
+import com.facebook.react.module.model.ReactModuleInfo;
+import com.facebook.react.module.model.ReactModuleInfoProvider;
 import com.facebook.react.uimanager.ViewManager;
 
-import java.util.Arrays;
 import java.util.Collections;
+import java.util.HashMap;
 import java.util.List;
+import java.util.Map;
 
 /**
  * PorcupinePackage class.
  */
-public class PorcupinePackage implements ReactPackage {
+public class PorcupinePackage extends TurboReactPackage {
     @Override
-    public List<NativeModule> createNativeModules(ReactApplicationContext reactContext) {
-        return Arrays.asList(new PorcupineModule(reactContext));
+    @Nullable
+    public NativeModule getModule(String name, ReactApplicationContext reactContext) {
+        if ("PvPorcupine".equals(name)) {
+            return new PorcupineModule(reactContext);
+        }
+        return null;
     }
 
     @Override
+    @NonNull
     public List<ViewManager> createViewManagers(ReactApplicationContext reactContext) {
         return Collections.emptyList();
     }
+
+    @Override
+    public ReactModuleInfoProvider getReactModuleInfoProvider() {
+        return () -> {
+            final Map<String, ReactModuleInfo> moduleInfos = new HashMap<>();
+            moduleInfos.put(
+                "PvPorcupine",
+                new ReactModuleInfo(
+                    "PvPorcupine",
+                    "PvPorcupine",
+                    false,
+                    false,
+                    false,
+                    false,
+                    false
+                )
+            );
+            return moduleInfos;
+        };
+    }
 }
diff --git a/node_modules/@picovoice/porcupine-react-native/src/porcupine.tsx b/node_modules/@picovoice/porcupine-react-native/src/porcupine.tsx
index dc3dc7b..c87be5d 100644
--- a/node_modules/@picovoice/porcupine-react-native/src/porcupine.tsx
+++ b/node_modules/@picovoice/porcupine-react-native/src/porcupine.tsx
@@ -13,7 +13,13 @@ import { NativeModules } from 'react-native';
 import * as PorcupineErrors from './porcupine_errors';
 import type BuiltInKeywords from './builtin_keywords';
 
-const RCTPorcupine = NativeModules.PvPorcupine;
+const getRCTPorcupine = () => {
+  const module = NativeModules.PvPorcupine;
+  if (!module) {
+    throw new Error('PvPorcupine native module is unavailable');
+  }
+  return module;
+};
 
 type NativeError = {
   code: string;
@@ -34,7 +40,7 @@ class Porcupine {
    */
   public static async getAvailableDevices() {
     try {
-      return await RCTPorcupine.getAvailableDevices();
+      return await getRCTPorcupine().getAvailableDevices();
     } catch (err) {
       if (err instanceof PorcupineErrors.PorcupineError) {
         throw err;
@@ -71,7 +77,7 @@ class Porcupine {
   ) {
     try {
       let { handle, frameLength, sampleRate, version } =
-        await RCTPorcupine.fromBuiltInKeywords(
+        await getRCTPorcupine().fromBuiltInKeywords(
           accessKey,
           modelPath,
           device,
@@ -114,7 +120,7 @@ class Porcupine {
   ) {
     try {
       let { handle, frameLength, sampleRate, version } =
-        await RCTPorcupine.fromKeywordPaths(
+        await getRCTPorcupine().fromKeywordPaths(
           accessKey,
           modelPath,
           device,
@@ -170,7 +176,7 @@ class Porcupine {
     }
 
     try {
-      return await RCTPorcupine.process(this._handle, frame);
+      return await getRCTPorcupine().process(this._handle, frame);
     } catch (err) {
       const nativeError = err as NativeError;
       throw Porcupine.codeToError(nativeError.code, nativeError.message);
@@ -181,7 +187,7 @@ class Porcupine {
    * Frees memory that was allocated for Porcupine
    */
   async delete() {
-    return RCTPorcupine.delete(this._handle);
+    return getRCTPorcupine().delete(this._handle);
   }
 
   /**
